VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# [2026-07-30 신규] 대한민국 공휴일 표시(캘린더)용 — api/holidays.js(서버리스 함수)에서만
# 사용하는 서버 전용 키. VITE_ 접두사를 붙이지 않는다(붙이면 브라우저 번들에 노출되어
# 보안 체크리스트 3번 항목에 위배됨). 로컬 개발 시 이 파일(.env)에 넣어도 되지만,
# 실제 배포(Vercel)에서는 Vercel 프로젝트 설정의 환경변수로 별도 등록해야 한다.
# 공공데이터포털(data.go.kr)에서 "특일 정보" 오픈API 신청 시 발급되는 서비스키를 넣는다.
# 비워두면 공휴일 표시만 비활성화되고 나머지 기능에는 영향이 없다.
HOLIDAY_API_KEY=
