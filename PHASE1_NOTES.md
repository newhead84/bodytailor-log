# Phase1 MVP — 신규 코드 착수 결과 (2026-07-27)

`BodyTailor_Log_앱설계안_v8_최종.md` 기준으로 실제 Vite + React + Firebase 코드를
처음부터 작성했습니다. 아래는 무엇이 되어 있고, 무엇이 아직 비어있는지에 대한
솔직한 정리입니다.

## 1. 구현된 것

- **인증**: Firebase Google 로그인/로그아웃 (`LoginScreen`)
- **온보딩**: 수준→성별→나이/몸무게/키→목표 스텝퍼 (`Onboarding`)
- **루틴 설정**: 5종 분할(무분할~5분할) 선택 → 파트별 종목 선택/직접추가 → 저장 (`RoutineSetup`)
- **홈 탭**: 티어/XP 진행바, 최근 기록 요약, AI 어드바이스 카드, 빠른 시작 버튼
- **기록 탭**
  - 입력: 파트별 종목 펼치기, 직전 기록 표시(자동 프리필용 조회), 세트 복사,
    스테퍼(+/−) 입력, 세트 저장 즉시 휴게타이머(1분/1분30초/2분) 자동 시작 + 종료 알림/진동,
    임시저장(localStorage)·새로고침 후 이어쓰기, 사이클/자유운동 구분(볼륨 가중치 0.7배)
  - 캘린더: 월간 캘린더 + 날짜별 상세 기록
  - 통계: 주간 총 볼륨 바차트, 이번 주 출석률, 종목별 중량 추이 라인차트
- **랭킹 탭**: 전체 랭킹 리스트, "내 점수 갱신"(출석 40%+볼륨 30%+과부하 30%) 버튼
- **MY 탭**: 닉네임 수정, 내 루틴 요약/재설정 진입, AI 모델(GPT/Gemini/Claude) 키 연동 +
  어드바이스 요청, 휴게타이머 알림 권한 요청, 로그아웃
- **디자인**: `BodyTailor_Log_디자인_가이드.md` 기준 Pretendard 폰트 + 화이트/블루/쿨그레이 토큰 적용
- **보안 기본기**: firestore.rules에서 본인 uid 데이터만 read/write 허용

## 2. 아직 비어있는 부분 (알고 있는 한계)

- **사이클 완료 판정 로직 없음**: `routineTemplates`의 `cycleCount`/`lastCycleCompletedAt`/
  `weeklyFrequencyLog`를 실제로 갱신하는 코드가 아직 없습니다. "모든 파트 최소 1회 수행 시
  사이클 완료" 판정과 다음 사이클 점진적 과부하 제안(8.4)은 다음 단계 작업입니다.
- **랭킹 보너스 미반영**: `computeStreakBonus`(연속 출석 보너스)는 함수만 만들어두고
  아직 "내 점수 갱신" 로직에 연결하지 않았습니다. 연속 출석을 추적하는 기준(주 단위 등)을
  먼저 정하고 붙이는 게 좋을 것 같습니다.
- **랭킹 점수의 신뢰성**: 지금은 클라이언트에서 점수를 계산해 그대로 Firestore에 씁니다.
  사용자가 마음만 먹으면 조작할 수 있는 구조라, 실사용자가 늘면 Cloud Functions로
  점수 계산을 옮기는 걸 권장합니다 (`src/utils/scoring.js` 상단 주석 참고).
- **AI 모델 연동의 보안**: API 키를 Firestore의 `users/{uid}.connectedAiModels`에 그대로
  저장하고, 브라우저에서 직접 GPT/Gemini/Claude API를 호출합니다. 프로토타입 단계로는
  동작하지만, 운영 단계에서는 서버 프록시 + 키 서버 보관으로 전환이 필요합니다
  (`src/utils/aiAdvice.js` 상단 주석 참고). OpenAI는 브라우저 CORS를 막아둘 가능성이 있어
  실제로는 GPT 연동만 별도 프록시가 먼저 필요할 수 있습니다.
- **`updateRoutineTemplate`, `updateWorkoutLog`, `parseSetString`**: storage.js/
  exerciseLibrary.js에 함수는 준비해 뒀지만 아직 어느 화면에서도 호출하지 않는
  "다음 기능을 위한 재료"입니다 (예: 루틴 수정 화면, 과거 기록 수정, 기존 텍스트 기록 일괄
  가져오기 등). 당장 안 써도 되지만, 죽은 코드로 오해하지 않으시라고 남겨둡니다.
- **Phase2 범위(트레이너 연동, 관리자 승인, 식단)**: 지침대로 이번 착수 범위에서 제외했습니다.
- **PWA 아이콘**: `public/manifest.json`의 아이콘은 비어 있습니다. 실제 배포 전 아이콘
  이미지를 추가해야 홈 화면 추가 시 아이콘이 제대로 보입니다.

## 3. 실행 방법

`README.md`의 Firebase 프로젝트 생성 → `.env` 설정 → `npm install` → `npm run dev` 순서를
그대로 따르면 됩니다. 빌드 검증은 이미 완료했습니다 (`npm run build` 성공).

## 4. 다음으로 이어가면 좋은 작업 (제안, 우선순위는 사용자 판단)

1. 사이클 완료 판정 + 다음 사이클 과부하 제안 로직
2. 랭킹 점수를 Cloud Functions로 이전 (조작 방지)
3. AI 모델 연동 보안 프록시 설계
4. PWA 아이콘 및 실제 배포(Firebase Hosting/Vercel) 검증
